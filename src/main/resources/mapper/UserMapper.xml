<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springboothello.dao.UserDao">
    <select id="findAllByIds" resultType="com.example.springboothello.pojo.User">
        select * from user
        <where>
            <foreach collection="ids" item="id" open="id in(" close=")" separator=",">
                #{id}
            </foreach>
        </where>

    </select>

    <select id="findAll" parameterType="com.example.springboothello.pojo.dto.UserDto"
            resultType="com.example.springboothello.pojo.User">
        select * from user
        <where>
            <if test="userDto.name!=null">
                and name =#{userDto.name}
            </if>
            <if test="userDto.ids!= null and userDto.ids.size() >0">
                and id in
                <foreach collection="userDto.ids" item="ids" separator="," open="(" close=")">
                    #{ids}
                </foreach>
            </if>
        </where>
    </select>
</mapper>

